/**
 * Prettier 配置
 * 与 ESLint 配置保持一致
 */
const prettierConfig = {
  semi: true,
  singleQuote: true,
  trailingComma: "es5",
  printWidth: 100,
  tabWidth: 2,
  useTabs: false,
  arrowParens: "always",
  endOfLine: "lf",
};

export default prettierConfig;
